/** Workflow
 *
 * 1 - Input: ddd: DDD, minutes: number, plan: plan
 * 2 - const totalWithPlan = calcuteTotal(ddd, minutes, plan?)
 * 3 - const totalWithoutPlan = calculateTotal(ddd, minutes)
 * 4 - return [totalWithPlan, totalWithouPlan]
 *
 */
